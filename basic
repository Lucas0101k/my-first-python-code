x = int(input("1.sayı:"))

y =int(input("2.sayı:"))  



toplam = int(x) + int(y)
print (toplam)

# eğer x in tipi ve y nin tipi tam sayı ise:

if type(x) == int and type(y) == int:

    print("Successfull")
